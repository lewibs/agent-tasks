{
	"name": "agent-tasks",
	"version": "1.0.0",
	"description": "Obsidian plugin: #agent-tagged tasks are completed by Claude Code running against your vault",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"author": "Lewbs",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^16.11.6",
		"builtin-modules": "3.3.0",
		"dotenv": "^16.3.1",
		"esbuild": "^0.18.0",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "4.7.4"
	}
}
